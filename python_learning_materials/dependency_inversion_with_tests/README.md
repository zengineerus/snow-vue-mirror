# Dependency Inversion *with tests*

Test the functionality of the *ReportGenerator* without the use of a database.

### Prerequesites:

Just needs pytest to run the tests.

    $ pip install pytest
    
### To run the tests

    $ pytest
    
