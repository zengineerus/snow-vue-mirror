{
  "name": "api",
  "version": "0.1.0",
  "description": "Api for Snow Vue App",
  "license": "NONE",
  "private": true,
  "scripts": {
    "start": "echo 'implement start api'",
    "test": "python3 -m unittest discover -p *_tests.py",
    "build": "echo 'implement build api'",
    "deploy": "sls deploy && sls downloadDocumentation --outputFileName=./swagger/SwaggerDocumentation.yml && aws s3 cp ./swagger/SwaggerDocumentation.yml s3://snowvue-swagger-docs/SwaggerDocumentation.yml"
  },
  "devDependencies": {
    "serverless-aws-documentation": "^1.1.0",
    "serverless-export-swagger": "^2.0.0"
  },
  "dependencies": {
    "serverless-aws-documentation": "^1.1.0"
  }
}
